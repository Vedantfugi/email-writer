# email-writer
AI-powered Chrome Extension that generates Gmail replies using the Gemini API and inserts them directly into the compose box.
**Email Writer Assistant** adds an **“AI Reply”** button directly to Gmail’s compose toolbar.  
When clicked, it automatically fetches the content of the received email, sends it to a Spring Boot backend integrated with **Google Gemini API**, and inserts the generated reply directly into the Gmail compose box.
